version: "2"
sql:
- schema: "data/migirations"
  queries: "data/query.sql"
  engine: "postgresql"
  database:
    uri:  postgresql://postgres:root@localhost:5432/user_db
  gen:
    go: 
      package: "rsqlc"
      out: "data/rsqlc"
      sql_package : database/sql
      emit_db_tags: true
      emit_interface: true 
      emit_exact_table_names: true 
      emit_json_tags: true 
      emit_sql_as_comment: true 

  